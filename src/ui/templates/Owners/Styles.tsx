import { StyleSheet } from 'react-native';
import { theme } from '@themes';

export const OwnersStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: theme.colors.background,
    padding: 16,
  },
});
